[build]
  command = "npm run build"
  publish = "dist"
  ignore = "git diff --quiet HEAD~1 -- src/ index.html public/ package.json vite.config.ts tsconfig.json tsconfig.app.json tsconfig.node.json netlify.toml"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
